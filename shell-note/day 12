4.3
磁盘监控脚本代码逐行注释

ther=80
设定变量预警值=80
size=$(df -h / | awk 'NR==2 {print $5}' | tr -d '%')
设定变量磁盘大小=磁盘数据表格化后强引用调取的第二行第五列对应的空间占比数据去除%后的纯数字数据 ""代表弱引用 ''代表强引用
if [ "$size" -gt "$ther" ]
-gt 数学对比参数“大于” 无法识别带有%的字符
then
     echo "磁盘空间不足"
else
     echo "正常,磁盘空间${size}%"
fi