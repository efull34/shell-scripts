3.30
创建新仓库必须在git根目录新建仓库名文件，映射新的git仓库网站，再进入新文件新建笔记文件

echo 打印
$ 调用参数
echo $! 打印上一条命令最后输出的状态 0为成功 其余失败
局部变量是定义在一个脚本中的变量，只能在这个脚本中使用 同时存在于相应的脚本中，才能令脚本正常运行
变量：定义=新建同义词 将新定义词条映射到已有的词条
具体语法为：
name=value 语法间不适用空格间隔
变量名=变量值
变量名和变量值可以是任何字符，但不能是数字和系统命令，同时特殊字符如空格内容需要使用" "强调
echo命令在打印变量名对应的值时 必须在变量名前添加$强调被调用的是一个变量名
示例：echo $name
为变量名添加{}可令其与后续输入的内容一起被打印出来
示例：echo ${name}123
重新输入语法即可更新变量的变量值
示例：name=new name
unset命令可删除变量
示例：unset name

if语法

if 条件
then 
    echo "命令"
elif 条件
then 
    echo "命令"
elif 条件
then 
    echo "命令"
.......
else "命令"
fi

条件命令格式中 (())中的内容适用于数学计算 同时可以添加 && 代表双重甚至多重条件 []中的内容进行非数字的条件判断 依照vim的语法切分规则 必须使用 空格间隔[]中的内容 间隔[] 但(())不需要被 空格间隔
在vim中编写脚本时 必须在首行明确需要调取的shell翻译版本：
#!/bin/bash
若为多条件if脚本 还需在第二行添加需要输出的打印内容：
read -p "   " test
打印出的内容在同样使用空格间隔 test在这里代表需要用一个特定的名称来代称用户或系统输入的值
./  运行脚本
脚本运行失败的原因除了脚本编写错误以外，最大的原因就是缺少执行权限，此时需要用chmod更改脚本的权限，当一个脚本默认显示为绿色时，代表这个脚本可以被正常执行
tset代指的值=用户与系统 输入与划分的所有值